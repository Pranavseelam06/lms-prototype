name: Keep Render Awake

on:
  schedule:
    - cron: "*/15 * * * *"   # every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render FastAPI
        run: |
          curl -s https://lms-prototype-1.onrender.com
